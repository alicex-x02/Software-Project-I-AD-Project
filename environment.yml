name: missing-vton
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - fastapi
      - uvicorn
      - pillow
      - pydantic
      - python-multipart
      - numpy
      - diffusers
      - safetensors
